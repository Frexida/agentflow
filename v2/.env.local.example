# ─────────────────────────────────────────────────
# AgentFlow — Self-Hosted Mode (.env.local)
# ─────────────────────────────────────────────────
# Copy this file to .env.local and fill in the values.

# OpenClaw Gateway (required)
# Your locally running OpenClaw Gateway URL
GATEWAY_INTERNAL_URL=ws://localhost:18789
GATEWAY_INTERNAL_TOKEN=your-gateway-token-here

# App URL (optional, defaults to http://localhost:3000)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ─── NOT REQUIRED in self-hosted mode ───────────
# The following are only needed if you want to enable
# cloud features (Supabase auth, billing, etc.)
# Leave these blank or omit them entirely.
#
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
